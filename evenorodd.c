#include <stdio.h>
void main()
{
    int;
    printf("Enter an integer");
    scanf("%d", &number);
    if(number % 4 == 0)
       {
       printf("%d",even number);
       }
    else
    {
        printf("%d",odd number);
        }
}
